/**
 * App configuration — swap values without hunting through UI code.
 * Donate URL is opened in the system browser (external).
 */
export const DONATE_URL = 'https://paypal.me/joeyboy215';

export const APP_VERSION = '1.0.0';
